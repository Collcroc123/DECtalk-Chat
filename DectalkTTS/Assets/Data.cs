using UnityEngine;

public class Data : MonoBehaviour
{
    //button that opens blacklist txt
    //check through blacklist before speaking if censored

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
